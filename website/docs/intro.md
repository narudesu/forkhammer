---
sidebar_position: 1
---

# forkhammer

`forkhammer` validates Jira issues against a codebase and creates an OpenCode session in a worktree.

Use it when you want a structured first pass before implementation.
